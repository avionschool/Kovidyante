<div class = "log-in-form">
    <div class = "column is-one-third">

      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="field">
          <%= f.label :email, class: "label"  %><br />
          <%= f.email_field :email,style: "margin: 0px; width: 583.4px; height: 50px;"  %>
        </div>

        <div class="field">
          <%= f.label :password, class: "label" %><br />
          <%= f.password_field :password, autocomplete: "current-password",style: "margin: 0px; width: 583.4px ; height: 50px;" %>
        </div>

        <% if devise_mapping.rememberable? %>
          <div class="field">
            <%= f.check_box :remember_me %>
            <%= f.label :remember_me %>
          </div>
        <% end %>

        <div class="actions">
          <%= f.submit "Log in",class: 'button is-primary is-inverted is-outlined is-medium' %>
        </div>
      <% end %>
      <%= render "devise/shared/links" %>
    </div>
  </div>

